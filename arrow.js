const doubleIt=num=>num*2;
const add = (x,y) => x+y;
const total=add(50,10);

const doMath=(x,y)=>{
    const sum=x+y;
    const diff=x-y;
    const result=(sum*diff);
    return result;
}
const result=doMath(50,10);
console.log(result)
